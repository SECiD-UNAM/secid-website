---
import ModernLayout from '../../layouts/ModernLayout.astro';
import Navigation from '../../components/Navigation.astro';
import Footer from '../../components/Footer.astro';
import { getTranslations } from '../../i18n/translations';

const t = getTranslations('en');

// Resources data
const resources = {
  learning: [
    {
      title: 'Python for Data Science',
      description:
        'Complete course from beginner to advanced with interactive notebooks',
      type: 'Course',
      level: 'Beginner',
      duration: '40 hours',
      icon: 'fab fa-python',
    },
    {
      title: 'Machine Learning Fundamentals',
      description: 'Learn fundamental ML concepts with practical examples',
      type: 'Tutorial',
      level: 'Intermediate',
      duration: '20 hours',
      icon: 'fas fa-robot',
    },
    {
      title: 'Deep Learning with PyTorch',
      description: 'Complete guide to building neural networks with PyTorch',
      type: 'Guide',
      level: 'Advanced',
      duration: '30 hours',
      icon: 'fas fa-brain',
    },
    {
      title: 'SQL for Data Analysis',
      description: 'Master SQL for complex queries and data analysis',
      type: 'Course',
      level: 'Beginner',
      duration: '15 hours',
      icon: 'fas fa-database',
    },
  ],
  tools: [
    {
      title: 'Jupyter Notebooks',
      description: 'Interactive environment for development and documentation',
      category: 'IDE',
      icon: 'fas fa-book',
      link: 'https://jupyter.org',
    },
    {
      title: 'Apache Spark',
      description: 'Framework for big data processing',
      category: 'Big Data',
      icon: 'fas fa-fire',
      link: 'https://spark.apache.org',
    },
    {
      title: 'TensorFlow',
      description: 'Open source platform for machine learning',
      category: 'ML Framework',
      icon: 'fas fa-chart-network',
      link: 'https://tensorflow.org',
    },
    {
      title: 'Docker',
      description: 'Containerization for development and deployment',
      category: 'DevOps',
      icon: 'fab fa-docker',
      link: 'https://docker.com',
    },
  ],
  datasets: [
    {
      title: 'Mexico Trade Dataset',
      description: 'Historical Mexican foreign trade data',
      size: '2.5 GB',
      format: 'CSV, JSON',
      records: '1M+',
    },
    {
      title: 'Spanish Tweets',
      description: 'Tweet corpus for sentiment analysis',
      size: '500 MB',
      format: 'JSON',
      records: '500K',
    },
    {
      title: 'Mexican Medical Images',
      description: 'Anonymized X-ray dataset',
      size: '10 GB',
      format: 'DICOM, PNG',
      records: '50K',
    },
    {
      title: 'Mexico City Weather Data',
      description: '10 years of meteorological data from Mexico City',
      size: '100 MB',
      format: 'CSV',
      records: '100K',
    },
  ],
  books: [
    {
      title: 'Pattern Recognition and Machine Learning',
      author: 'Christopher Bishop',
      year: 2006,
      level: 'Advanced',
      icon: 'fas fa-book-open',
    },
    {
      title: 'The Elements of Statistical Learning',
      author: 'Hastie, Tibshirani, Friedman',
      year: 2009,
      level: 'Advanced',
      icon: 'fas fa-book-open',
    },
    {
      title: 'Python Data Science Handbook',
      author: 'Jake VanderPlas',
      year: 2016,
      level: 'Intermediate',
      icon: 'fas fa-book-open',
    },
    {
      title: 'Deep Learning',
      author: 'Ian Goodfellow',
      year: 2016,
      level: 'Advanced',
      icon: 'fas fa-book-open',
    },
  ],
};
---

<ModernLayout
  title="Resources - SECiD"
  description="Educational resources, tools and datasets for data science professionals"
  lang="en"
>
  <Navigation lang="en" />

  <main>
    <!-- Hero Section -->
    <section class="secid-hero secid-hero--secondary">
      <div class="secid-hero__container">
        <div class="secid-hero__content">
          <h1 class="secid-hero__title">Resource Center</h1>
          <p class="secid-hero__subtitle">
            Everything you need to grow in data science
          </p>
        </div>
      </div>
    </section>

    <!-- Learning Resources -->
    <section class="secid-section">
      <div class="secid-container">
        <div class="secid-section__header">
          <h2 class="secid-section__title">
            <i class="fas fa-graduation-cap"></i>
            Learning Resources
          </h2>
          <p class="secid-section__description">
            Courses, tutorials and guides curated by the community
          </p>
        </div>

        <div class="secid-grid secid-grid--2">
          {
            resources.learning.map((resource) => (
              <div class="secid-resource-card">
                <div class="secid-resource-card__header">
                  <div class="secid-resource-card__icon">
                    <i class={resource.icon} />
                  </div>
                  <div class="secid-resource-card__meta">
                    <span class="secid-resource-card__type">
                      {resource.type}
                    </span>
                    <span
                      class="secid-resource-card__level"
                      data-level={resource.level.toLowerCase()}
                    >
                      {resource.level}
                    </span>
                  </div>
                </div>

                <h3 class="secid-resource-card__title">{resource.title}</h3>
                <p class="secid-resource-card__description">
                  {resource.description}
                </p>

                <div class="secid-resource-card__footer">
                  <span class="secid-resource-card__duration">
                    <i class="fas fa-clock" />
                    {resource.duration}
                  </span>
                  <a
                    href="#"
                    class="secid-button secid-button--primary secid-button--sm"
                  >
                    Access
                  </a>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Tools & Software -->
    <section class="secid-section secid-bg-light">
      <div class="secid-container">
        <div class="secid-section__header">
          <h2 class="secid-section__title">
            <i class="fas fa-tools"></i>
            Tools & Software
          </h2>
          <p class="secid-section__description">
            The best tools for your daily work
          </p>
        </div>

        <div class="secid-grid secid-grid--4">
          {
            resources.tools.map((tool) => (
              <a
                href={tool.link}
                target="_blank"
                rel="noopener noreferrer"
                class="secid-tool-card"
              >
                <div class="secid-tool-card__icon">
                  <i class={tool.icon} />
                </div>
                <h3 class="secid-tool-card__title">{tool.title}</h3>
                <p class="secid-tool-card__category">{tool.category}</p>
                <p class="secid-tool-card__description">{tool.description}</p>
                <span class="secid-tool-card__link">
                  Visit <i class="fas fa-external-link-alt" />
                </span>
              </a>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Datasets -->
    <section class="secid-section">
      <div class="secid-container">
        <div class="secid-section__header">
          <h2 class="secid-section__title">
            <i class="fas fa-database"></i>
            Datasets
          </h2>
          <p class="secid-section__description">
            Data collections for practicing and developing projects
          </p>
        </div>

        <div class="secid-datasets">
          {
            resources.datasets.map((dataset) => (
              <div class="secid-dataset-card">
                <div class="secid-dataset-card__header">
                  <h3 class="secid-dataset-card__title">{dataset.title}</h3>
                  <button class="secid-button secid-button--primary secid-button--sm">
                    <i class="fas fa-download" />
                    Download
                  </button>
                </div>

                <p class="secid-dataset-card__description">
                  {dataset.description}
                </p>

                <div class="secid-dataset-card__stats">
                  <div class="secid-dataset-card__stat">
                    <i class="fas fa-hdd" />
                    <span>{dataset.size}</span>
                  </div>
                  <div class="secid-dataset-card__stat">
                    <i class="fas fa-file-code" />
                    <span>{dataset.format}</span>
                  </div>
                  <div class="secid-dataset-card__stat">
                    <i class="fas fa-list-ol" />
                    <span>{dataset.records} records</span>
                  </div>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Books & Publications -->
    <section class="secid-section secid-bg-cream">
      <div class="secid-container">
        <div class="secid-section__header">
          <h2 class="secid-section__title">
            <i class="fas fa-books"></i>
            Recommended Books
          </h2>
          <p class="secid-section__description">
            Essential literature for data science professionals
          </p>
        </div>

        <div class="secid-grid secid-grid--4">
          {
            resources.books.map((book) => (
              <div class="secid-book-card">
                <div class="secid-book-card__cover">
                  <i class={book.icon} />
                </div>
                <h3 class="secid-book-card__title">{book.title}</h3>
                <p class="secid-book-card__author">{book.author}</p>
                <div class="secid-book-card__meta">
                  <span class="secid-book-card__year">{book.year}</span>
                  <span class="secid-book-card__level">{book.level}</span>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="secid-section">
      <div class="secid-container">
        <div class="secid-resources__cta">
          <i class="fas fa-plus-circle secid-resources__cta-icon"></i>
          <h2 class="secid-resources__cta-title">Have a resource to share?</h2>
          <p class="secid-resources__cta-description">
            Help the community by sharing tools, datasets or educational
            materials
          </p>
          <button class="secid-button secid-button--primary secid-button--lg">
            <i class="fas fa-share"></i>
            Share Resource
          </button>
        </div>
      </div>
    </section>
  </main>

  <Footer lang="en" />
</ModernLayout>

<style>
  /* Resource Cards */
  .secid-resource-card {
    background: var(--card-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-xl);
    transition: all var(--transition-base);
  }

  .secid-resource-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--secid-primary);
  }

  .secid-resource-card__header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: var(--space-lg);
  }

  .secid-resource-card__icon {
    width: 3rem;
    height: 3rem;
    background: linear-gradient(
      135deg,
      var(--secid-primary),
      var(--secid-gold)
    );
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.5rem;
  }

  .secid-resource-card__meta {
    display: flex;
    gap: var(--space-sm);
  }

  .secid-resource-card__type {
    padding: var(--space-xs) var(--space-sm);
    background: var(--color-surface-alt);
    border-radius: var(--radius-md);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-text-secondary);
  }

  .secid-resource-card__level {
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-md);
    font-size: 0.75rem;
    font-weight: 600;
  }

  .secid-resource-card__level[data-level='beginner'] {
    background: rgba(34, 197, 94, 0.1);
    color: var(--secid-success);
  }

  .secid-resource-card__level[data-level='intermediate'] {
    background: rgba(251, 191, 36, 0.1);
    color: var(--secid-warning);
  }

  .secid-resource-card__level[data-level='advanced'] {
    background: rgba(239, 68, 68, 0.1);
    color: var(--secid-error);
  }

  .secid-resource-card__title {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 0 0 var(--space-sm) 0;
  }

  .secid-resource-card__description {
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0 0 var(--space-lg) 0;
  }

  .secid-resource-card__footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .secid-resource-card__duration {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    color: var(--color-text-tertiary);
    font-size: 0.875rem;
  }

  /* Tool Cards */
  .secid-tool-card {
    background: var(--card-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-lg);
    text-align: center;
    text-decoration: none;
    color: inherit;
    transition: all var(--transition-base);
    display: block;
  }

  .secid-tool-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--secid-primary);
  }

  .secid-tool-card__icon {
    width: 3rem;
    height: 3rem;
    background: linear-gradient(
      135deg,
      var(--secid-secondary),
      var(--secid-secondary-dark)
    );
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.5rem;
    margin: 0 auto var(--space-md);
  }

  .secid-tool-card__title {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 0 0 var(--space-xs) 0;
  }

  .secid-tool-card__category {
    font-size: 0.75rem;
    color: var(--secid-secondary);
    font-weight: 600;
    text-transform: uppercase;
    margin: 0 0 var(--space-sm) 0;
  }

  .secid-tool-card__description {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    line-height: 1.5;
    margin: 0 0 var(--space-md) 0;
  }

  .secid-tool-card__link {
    color: var(--secid-primary);
    font-size: 0.875rem;
    font-weight: 500;
  }

  /* Dataset Cards */
  .secid-datasets {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(400px, 1fr));
    gap: var(--space-xl);
  }

  .secid-dataset-card {
    background: var(--card-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
  }

  .secid-dataset-card__header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: var(--space-md);
  }

  .secid-dataset-card__title {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 0;
  }

  .secid-dataset-card__description {
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0 0 var(--space-lg) 0;
  }

  .secid-dataset-card__stats {
    display: flex;
    gap: var(--space-lg);
  }

  .secid-dataset-card__stat {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
  }

  /* Book Cards */
  .secid-book-card {
    background: var(--card-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-lg);
    text-align: center;
    transition: all var(--transition-base);
  }

  .secid-book-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .secid-book-card__cover {
    width: 4rem;
    height: 5rem;
    background: linear-gradient(
      135deg,
      var(--secid-gold),
      var(--secid-gold-dark)
    );
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 2rem;
    margin: 0 auto var(--space-md);
  }

  .secid-book-card__title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 0 0 var(--space-xs) 0;
    min-height: 2.5rem;
  }

  .secid-book-card__author {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    margin: 0 0 var(--space-md) 0;
  }

  .secid-book-card__meta {
    display: flex;
    justify-content: center;
    gap: var(--space-md);
    font-size: 0.75rem;
  }

  .secid-book-card__year {
    color: var(--color-text-tertiary);
  }

  .secid-book-card__level {
    color: var(--secid-primary);
    font-weight: 600;
  }

  /* CTA */
  .secid-resources__cta {
    text-align: center;
    padding: var(--space-3xl);
    background: linear-gradient(
      135deg,
      var(--secid-primary),
      var(--secid-gold)
    );
    border-radius: var(--radius-xl);
    color: white;
  }

  .secid-resources__cta-icon {
    font-size: 3rem;
    margin-bottom: var(--space-lg);
  }

  .secid-resources__cta-title {
    font-size: 2rem;
    font-weight: 700;
    margin: 0 0 var(--space-md) 0;
  }

  .secid-resources__cta-description {
    font-size: 1.125rem;
    margin: 0 0 var(--space-xl) 0;
    opacity: 0.95;
  }

  /* Mobile */
  @media (max-width: 768px) {
    .secid-datasets {
      grid-template-columns: 1fr;
    }

    .secid-dataset-card__header {
      flex-direction: column;
      gap: var(--space-md);
    }

    .secid-dataset-card__stats {
      flex-direction: column;
      gap: var(--space-sm);
    }
  }
</style>
