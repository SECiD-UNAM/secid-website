---
import ModernLayout from '../../layouts/ModernLayout.astro';
import Navigation from '../../components/Navigation.astro';
import Footer from '../../components/Footer.astro';
---

<ModernLayout
  title="Mentorship Program - SECiD"
  description="Connect with experienced data science mentors or become a mentor for the SECiD community"
  lang="en"
>
  <Navigation lang="en" />

  <main>
    <section class="secid-hero secid-hero--secondary">
      <div class="secid-hero__container">
        <div class="secid-hero__content">
          <h1 class="secid-hero__title">Mentorship Program</h1>
          <p class="secid-hero__subtitle">
            Connect with experienced mentors or guide the next generation of data scientists
          </p>
        </div>
      </div>
    </section>

    <!-- How it works -->
    <section class="secid-section">
      <div class="secid-container">
        <div class="secid-section__header" style="text-align: center; margin-bottom: 3rem;">
          <h2 class="secid-section__title">How does it work?</h2>
          <p class="secid-section__description">Our mentorship program connects experienced professionals with members seeking guidance</p>
        </div>

        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem; margin-bottom: 3rem;">
          <div style="text-align: center; padding: 2rem; background: var(--card-bg); border: 1px solid var(--color-border); border-radius: var(--radius-xl);">
            <div style="width: 4rem; height: 4rem; background: linear-gradient(135deg, var(--secid-primary), var(--secid-gold)); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; color: white; font-size: 1.5rem;">
              <i class="fas fa-user-plus"></i>
            </div>
            <h3 style="font-size: 1.25rem; font-weight: 600; margin-bottom: 0.5rem;">1. Sign Up</h3>
            <p style="color: var(--color-text-secondary);">Create your profile as a mentor or mentee and define your areas of interest</p>
          </div>

          <div style="text-align: center; padding: 2rem; background: var(--card-bg); border: 1px solid var(--color-border); border-radius: var(--radius-xl);">
            <div style="width: 4rem; height: 4rem; background: linear-gradient(135deg, var(--secid-secondary), var(--secid-secondary-dark)); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; color: white; font-size: 1.5rem;">
              <i class="fas fa-handshake"></i>
            </div>
            <h3 style="font-size: 1.25rem; font-weight: 600; margin-bottom: 0.5rem;">2. Matching</h3>
            <p style="color: var(--color-text-secondary);">Our algorithm finds the best matches based on skills, availability, and goals</p>
          </div>

          <div style="text-align: center; padding: 2rem; background: var(--card-bg); border: 1px solid var(--color-border); border-radius: var(--radius-xl);">
            <div style="width: 4rem; height: 4rem; background: linear-gradient(135deg, var(--secid-gold), var(--secid-gold-dark)); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; color: white; font-size: 1.5rem;">
              <i class="fas fa-rocket"></i>
            </div>
            <h3 style="font-size: 1.25rem; font-weight: 600; margin-bottom: 0.5rem;">3. Grow</h3>
            <p style="color: var(--color-text-secondary);">Participate in regular sessions, set goals, and advance your professional career</p>
          </div>
        </div>

        <!-- CTAs -->
        <div style="display: flex; gap: 1.5rem; justify-content: center; flex-wrap: wrap;">
          <a href="/en/mentorship/browse" class="secid-button secid-button--primary secid-button--lg">
            <i class="fas fa-search"></i>
            Find Mentors
          </a>
          <a href="/en/dashboard" class="secid-button secid-button--outline secid-button--lg">
            <i class="fas fa-chalkboard-teacher"></i>
            Become a Mentor
          </a>
        </div>
      </div>
    </section>

    <!-- Stats -->
    <section class="secid-section" style="background: var(--color-surface-alt);">
      <div class="secid-container">
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 2rem; text-align: center;">
          <div>
            <div style="font-size: 2.5rem; font-weight: 700; color: var(--secid-primary);">50+</div>
            <div style="color: var(--color-text-secondary);">Active Mentors</div>
          </div>
          <div>
            <div style="font-size: 2.5rem; font-weight: 700; color: var(--secid-secondary);">120+</div>
            <div style="color: var(--color-text-secondary);">Completed Sessions</div>
          </div>
          <div>
            <div style="font-size: 2.5rem; font-weight: 700; color: var(--secid-gold);">4.8/5</div>
            <div style="color: var(--color-text-secondary);">Average Rating</div>
          </div>
          <div>
            <div style="font-size: 2.5rem; font-weight: 700; color: var(--secid-primary);">85%</div>
            <div style="color: var(--color-text-secondary);">Satisfaction Rate</div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer lang="en" />
</ModernLayout>
